import React from "react";
import {
  Text,
  View,
  TextInput,
  Button,
  Alert,
  KeyboardAvoidingView,
  TouchableOpacity,
  ActivityIndicator,
} from "react-native";
const Delivery = () => {
  return (
    <View>
      <Text>Delivery</Text>
    </View>
  );
};

export default React.memo(Delivery);
